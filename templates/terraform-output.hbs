---
steps:
  - name: terraform
    image: hashicorp/terraform
    environment:
      GOOGLE_CREDENTIALS:
        from_secret: gcp_credentials
    commands:
      - cd {{choose work_dir '.'}}
      - $VARIABLE=`terraform output -raw {{output}}`
      - echo $VARIABLE > {{file}}
