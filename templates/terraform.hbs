---
steps:
  - name: {{choose name 'terraform-apply'}}
    image: hashicorp/terraform
    environment:
      GOOGLE_CREDENTIALS:
        from_secret: gcp_credentials
    commands:
      - cd {{choose work_dir '.'}}
      - terraform init
      - terraform validate
      - terraform apply -auto-approve
